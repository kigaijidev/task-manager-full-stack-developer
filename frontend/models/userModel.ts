type User = {
  _id: string;
  name: string;
  email: string;
  isAuth: boolean;
  currency: Number;
  plan: string;
  expiredDay: string;
  lastAccess: string;
  fullName: string;
  bank: string;
  bankNumber: string;
};

export default User;
